This application is windows only.


Setting up Gemini API key

1. Sign up for Gemini API and get your key.
2. Set the environment variable before running the app:

**Windows (PowerShell):**
setx GEMINI_API_KEY "your_api_key_here"

3. Run the app. The API key will be read automatically.